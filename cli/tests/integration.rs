#[cfg(test)]
mod tests {
    use nprint_types::Artifact;

    #[test]
    fn test_deploy() {
        let _mock_contract = Artifact {
            script: vec![],
            props: vec![],
        };
        // Further test code can be added here
    }
}
